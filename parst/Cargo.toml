[package]
name = "parst"
version = "0.1.5"
edition = "2021"
description = "declarative parsing"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = "1.0.30"
parst_derive = { version = "0.1.3", path = "../parst_derive" , optional = true }

[features]
default = ["derive"]
derive = ["parst_derive"]