[package]
name = "parst"
version = "0.1.4"
edition = "2021"
description = "declarative parsing"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror = "1.0.30"
parst_derive = { path = "../parst_derive" , optional = true }

[features]
default = ["derive"]
derive = ["parst_derive"]